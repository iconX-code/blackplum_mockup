# the name by which the project can be referenced within Serena
project_name: "blackplum_mockup"


# list of languages for which language servers are started; choose from:
#   al                  ansible             bash                clojure             cpp
#   cpp_ccls            crystal             csharp              csharp_omnisharp    dart
#   elixir              elm                 erlang              fortran             fsharp
#   go                  groovy              haskell             haxe                hlsl
#   java                json                julia               kotlin              lean4
#   lua                 luau                markdown            matlab              msl
#   nix                 ocaml               pascal              perl                php
#   php_phpactor        powershell          python              python_jedi         python_ty
#   r                   rego                ruby                ruby_solargraph     rust
#   scala               solidity            swift               systemverilog       terraform
#   toml                typescript          typescript_vts      vue                 yaml
#   zig
#   (This list may be outdated. For the current list, see values of Language enum here:
#   https://github.com/oraios/serena/blob/main/src/solidlsp/ls_config.py
#   For some languages, there are alternative language servers, e.g. csharp_omnisharp, ruby_solargraph.)
# Note:
#   - For C, use cpp
#   - For JavaScript, use typescript
#   - For Free Pascal/Lazarus, use pascal
# Special requirements:
#   Some languages require additional setup/installations.
#   See here for details: https://oraios.github.io/serena/01-about/020_programming-languages.html#language-servers
# When using multiple languages, the first language server that supports a given file will be used for that file.
# The first language is the default language and the respective language server will be used as a fallback.
# Note that when using the JetBrains backend, language servers are not used and this list is correspondingly ignored.
languages:
- typescript

# the encoding used by text files in the project
# For a list of possible encodings, see https://docs.python.org/3.11/library/codecs.html#standard-encodings
encoding: "utf-8"

# line ending convention to use when writing source files.
# Possible values: unset (use global setting), "lf", "crlf", or "native" (platform default)
# This does not affect Serena's own files (e.g. memories and configuration files), which always use native line endings.
line_ending:

# The language backend to use for this project.
# If not set, the global setting from serena_config.yml is used.
# Valid values: LSP, JetBrains
# Note: the backend is fixed at startup. If a project with a different backend
# is activated post-init, an error will be returned.
language_backend:

# whether to use project's .gitignore files to ignore files
ignore_all_files_in_gitignore: true

# advanced configuration option allowing to configure language server-specific options.
# Maps the language key to the options.
# Have a look at the docstring of the constructors of the LS implementations within solidlsp (e.g., for C# or PHP) to see which options are available.
# No documentation on options means no options are available.
ls_specific_settings: {}

# list of additional paths to ignore in this project.
# Same syntax as gitignore, so you can use * and **.
# Note: global ignored_paths from serena_config.yml are also applied additively.
ignored_paths:
  - "*.bak"
  - "*.md.bak"
  - "design_ref/**"

# whether the project is in read-only mode
# If set to true, all editing tools will be disabled and attempts to use them will result in an error
# Added on 2025-04-18
read_only: false

# list of tool names to exclude.
# This extends the existing exclusions (e.g. from the global configuration)
# Find the list of tools here: https://oraios.github.io/serena/01-about/035_tools.html
excluded_tools: []

# list of tools to include that would otherwise be disabled (particularly optional tools that are disabled by default).
# This extends the existing inclusions (e.g. from the global configuration).
# Find the list of tools here: https://oraios.github.io/serena/01-about/035_tools.html
included_optional_tools: []

# fixed set of tools to use as the base tool set (if non-empty), replacing Serena's default set of tools.
# This cannot be combined with non-empty excluded_tools or included_optional_tools.
# Find the list of tools here: https://oraios.github.io/serena/01-about/035_tools.html
fixed_tools: []

# list of mode names that are to be activated by default, overriding the setting in the global configuration.
# The full set of modes to be activated is base_modes (from global config) + default_modes + added_modes.
# If the setting is undefined/empty, the default_modes from the global configuration (serena_config.yml) apply.
# Otherwise, this overrides the setting from the global configuration (serena_config.yml).
# Therefore, you can set this to [] if you do not want the default modes defined in the global config to apply
# for this project.
# This setting can, in turn, be overridden by CLI parameters (--mode).
# See https://oraios.github.io/serena/02-usage/050_configuration.html#modes
default_modes:

# list of mode names to be activated additionally for this project, e.g. ["query-projects"]
# The full set of modes to be activated is base_modes (from global config) + default_modes + added_modes.
# See https://oraios.github.io/serena/02-usage/050_configuration.html#modes
added_modes:

# initial prompt for the project. It will always be given to the LLM upon activating the project
# (contrary to the memories, which are loaded on demand).
initial_prompt: |
  blackplum_mockup (내부 코드명: INBOXX) — Instagram DM/댓글, YouTube 댓글, Gmail, 틱톡, 네이버 메일 등 SNS 메시지를 자동 분류해 처리해야 할 일 큐로 보여주는 SaaS의 working mockup. 한국 IG/유튜브 중형 크리에이터 시연용. 모바일 UI 품질 최우선.

  기술 스택: React 18 (CDN UMD) + Babel Standalone (브라우저 트랜스파일) + Pure CSS, 단일 `index.html`을 브라우저로 직접 로드. 빌드 도구 없음. GitHub Pages 정적 호스팅.

  핵심 파일:
  - `index.html` — 모든 React 컴포넌트 (script type="text/babel" 블록 안에 작성)
  - `styles.css` — 3-tier 디자인 토큰 시스템 (Tier 1 primitive / Tier 2 semantic / Tier 3 component)
  - `mock-data.js` — `window.MOCK_DATA` 하드코딩 샘플 데이터 (한국어 페르소나)
  - `icon-set.js` — `window.ICON_SET` SVG registry, `<Icon name="..." />`로만 사용
  - `spec.md` (~91KB) / `req.md` (~44KB) / `tasks.md` (~20KB) — 기술 스펙 / 요구사항 / 진행 현황
  - `.claude/CLAUDE.md` — 세션 행동 규칙 (sub-agent 위임 정책 포함)

  절대 원칙 (위반 시 swap 비용 폭발):
  1. 디자인 토큰: 컴포넌트 코드는 Tier 2(semantic) 또는 Tier 3(component) 토큰만 참조. Tier 1 primitive 직접 참조 금지. raw 값(hex / px / rem / 폰트명 / radius / shadow / motion duration) 하드코딩 절대 금지. 상세는 spec.md §3.
  2. 데이터 schema: spec.md §4 기준. mock-data.js 변경 시 §4-11 분포 준수.
  3. 아이콘: SVG inline 금지, 반드시 `<Icon />` 컴포넌트 경유. raw px 사이즈 금지(--icon-size-* 토큰만).
  4. JSX 내 한국어 텍스트 사용 (시연 대상이 한국 사용자).

  대용량 markdown 핸들링:
  - spec.md / req.md를 통째로 Read하면 컨텍스트 폭발 + watchdog stall 사고 패턴(실제 발생 이력). 정확한 섹션 또는 라인 범위로 핀포인트 Read하거나 Serena 심볼/검색 도구 활용.

  코드 수정 위임 (Claude Code 세션에서):
  - frontend-dev → index.html / styles.css / icon-set.js
  - mock-data-writer → mock-data.js
  - code-reviewer → 코드 수정 후 읽기 전용 검증
  자세한 위임 규칙은 `.claude/CLAUDE.md` 참조.

# time budget (seconds) per tool call for the retrieval of additional symbol information
# such as docstrings or parameter information.
# This overrides the corresponding setting in the global configuration; see the documentation there.
# If null or missing, use the setting from the global configuration.
symbol_info_budget:

# list of regex patterns which, when matched, mark a memory entry as read‑only.
# Extends the list from the global configuration, merging the two lists.
read_only_memory_patterns: []

# list of regex patterns for memories to completely ignore.
# Matching memories will not appear in list_memories or activate_project output
# and cannot be accessed via read_memory or write_memory.
# To access ignored memory files, use the read_file tool on the raw file path.
# Extends the list from the global configuration, merging the two lists.
# Example: ["_archive/.*", "_episodes/.*"]
ignored_memory_patterns: []
